<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.skio.demo.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="cn.skio.demo.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, email
    </sql>

    <select id="count" resultType="java.lang.Integer">
        select count(1) from users
    </select>

    <insert id="save" parameterType="cn.skio.demo.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into users(username,email)values(#{username},#{email})
    </insert>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM users
    </select>

    <select id="findUserById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id}
    </select>
</mapper>